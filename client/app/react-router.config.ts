import type { Config } from "@react-router/dev/config";

export default {
	appDirectory: "app", // This should point to your 'app' folder
	// ssr: false, // Set to true if you're doing SSR
} satisfies Config;
